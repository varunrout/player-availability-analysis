# Analysis Outputs

Retained analysis artifacts generated by approved scripts belong under one nested directory per stage.

Use `figures/`, `tables/`, `reports/` and `metadata/` subdirectories when applicable. Notebook-rendered outputs are not stored here.
