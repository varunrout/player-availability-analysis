# Analysis Jobs

Each approved pre-model analysis stage receives a reproducible command-line runner here.

Runners import shared logic from `player_availability.analysis` and write retained artifacts only beneath `outputs/analysis/<stage>/`.
