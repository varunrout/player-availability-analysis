# Local development overrides.
#
# Active when PAA_ENV is unset or set to "local".
# Keep this file minimal: values that differ here but not in dev or prod are a
# common source of results that cannot be reproduced elsewhere.

analysis: {}
